export enum SHAPE {
  Wave = 1,
  Eye = 2,
  Star = 3,
  Portal = 4,
  Net = 5,
}

export enum COLOR {
  Magenta = 1,
  Red = 2,
  Orange = 3,
  White = 4,
}

export enum COLOR_HEX {
  '#ff00ff' = COLOR.Magenta,
  '#ff0000' = COLOR.Red,
  '#ff6600' = COLOR.Orange,
  '#ffffff' = COLOR.White,
}
